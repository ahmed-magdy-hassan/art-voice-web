# Browser-visible (NUXT_PUBLIC_*)
NUXT_PUBLIC_SITE_URL=http://localhost:3001
NUXT_PUBLIC_OTEL_COLLECTOR_URL=
NUXT_PUBLIC_SENTRY_DSN=

# Server-only — Nitro BFF reaches upstream services via these URLs.
# In docker compose this resolves to the service hostname on the art-voice network.
BUSINESS_URL=http://svc-business-monolith:3000
SERVICE_JWT_SECRET=
