// shadcn-vue style Tooltip — reka-ui primitives + Art-Voice design tokens.
// Wrap the app (or a subtree) in TooltipProvider once.
export {
  TooltipProvider,
  TooltipRoot,
  TooltipTrigger,
} from 'reka-ui'
export { default as TooltipContent } from './TooltipContent.vue'
